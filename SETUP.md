# Brewwery GitHub Organization Profile

Create a public repository named `.github` inside the `brewwery` GitHub organization.

Then place this structure inside it:

```txt
.github/
└── profile/
    ├── README.md
    └── assets/
        └── brewwery-banner.png
```

GitHub will automatically render `profile/README.md` on the organization profile page.
